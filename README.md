WIP
---
Major revisions to come

---
````
usage: mlab_cockpit.py [-h] [-s] [file/dir]

reads ML_AB files and displays various statistics

positional arguments:
  file/dir        file to read - if directory is supplied, looks for ML_AB
                  file - defaults to working directory

options:
  -h, --help      show this help message and exit
  -s, --separate  produces separate figure for each plot
````